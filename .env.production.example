# Copy to .env.production on VPS and fill secrets there. Do not commit the real file.
RAS_API_PRODUCTION_PORT=8093
RAS_DB_PATH=/data/ras-store.json
ZERNIO_MODE=dry-run
ZERNIO_BASE_URL=https://zernio.com/api/v1
ZERNIO_API_KEY=
ZERNIO_TIMEOUT_MS=30000
RAS_WORKER_BATCH_SIZE=20
RAS_WORKER_IDLE_MS=5000
RAS_WORKER_MAX_RETRIES=5
RAS_WORKER_BASE_RETRY_MS=60000

# Dedicated RAS Redis only. Generate URL-safe value: openssl rand -hex 32
RAS_REDIS_PASSWORD=
RAS_PAT_RATE_LIMIT_PER_MINUTE=120

ZERNIO_WEBHOOK_SECRET=
